apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-test
  namespace: exam
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
