apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-test
  namespace: exam
spec:
  storageClassName: standard
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/docker/mnt"
